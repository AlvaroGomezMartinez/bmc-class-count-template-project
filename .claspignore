# clasp pushes everything inside rootDir (./src) by default.
# This file exists to explicitly document what is excluded.

# The tests/ folder lives outside src/ and is never pushed.
# No additional exclusions needed within src/.
